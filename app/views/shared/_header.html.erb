<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Media collection</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li><a href="<%= user_path(current_user.id) %>" style="padding: 0;"><%= image_tag(current_user.image) %></a></li>
          <li><a href="/sessions/destroy">Exit</a></li>
        <% else %>
          <li><a href="/auth/vkontakte">Login with VK</a></li>
          <li><a href="/auth/facebook">Login with FB</a></li>
        <% end %>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>